# Models

This directory contains the fine-tuned machine learning models used for CVE-to-CWE classification.

## Model Source

The models stored here are typically generated using the **CVE-LMTune** tool or other training pipelines. Each model should be placed in its own subdirectory.